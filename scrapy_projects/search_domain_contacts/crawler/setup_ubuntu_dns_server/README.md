## Run setup DNS server by
`sudo python3 setup_ubuntu_dns_server/main.py`

## How to change the ubuntu root password (optional)
switch to root with:
`$ sudo su -`

Then run:
`$ passwd ubuntu`

It is going to prompt :
    Enter new UNIX password:

So, set your password.