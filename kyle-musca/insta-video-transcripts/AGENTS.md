# Agent notes

When Instaloader or `instagram_reels_transcripts.py` hits 429, `web_profile_info`, GraphQL `execution error`, stale `doc_id`, or empty reel listing, follow the project skill:

`skills/debugging-instagram-429/SKILL.md`
